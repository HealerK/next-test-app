"use client";
const AddToCart = () => {
  return (
    <button
      className="btn btn-primary"
      onClick={() => console.log("Product to add.")}
    >
      Add to cart
    </button>
  );
};

export default AddToCart;
